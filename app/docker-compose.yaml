version: "3.12"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: home-task-revolut:1.0.9
    ports: 
      - 80:5000
    restart: always
    networks:
      - webnet

networks:
  webnet:

